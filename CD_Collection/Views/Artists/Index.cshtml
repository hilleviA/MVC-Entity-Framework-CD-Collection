@model IEnumerable<CD_Collection.Models.Artist>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArtistName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfBirth)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecordCompany)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ArtistName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfBirth)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecordCompany)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArtistName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ArtistName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ArtistName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
